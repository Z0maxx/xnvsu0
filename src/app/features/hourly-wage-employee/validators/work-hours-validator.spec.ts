import { WorkHoursValidator } from './work-hours-validator';

describe('WorkHoursValidator', () => {
  it('should create an instance', () => {
    expect(new WorkHoursValidator()).toBeTruthy();
  });
});
